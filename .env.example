# Copy this file to .env and fill in real values. .env is gitignored - never commit it.

# Oracle connection (python-oracledb, thin mode - no Oracle Client install needed)
ORACLE_USER=retaildemo
ORACLE_PASSWORD=
ORACLE_DSN=DESKTOP-M4R2VLU:1521/FREEPDB1

# Anthropic API key, used by `oia ask` for the natural-language agent
ANTHROPIC_API_KEY=
